apiVersion: v1
kind: Service
metadata:
  name: habitapp
  namespace: habitapp
spec:
  selector:
    app: habitapp
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
